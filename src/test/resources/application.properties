# spring config
springconfig=test
# Enable lazy load on view
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
# Disable flyway to run after update on schema by hibernate
spring.flyway.enabled=false